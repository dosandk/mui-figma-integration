import { createTheme } from '@mui/material';

export const eleksTheme = createTheme({
  cssVariables: true,
  palette: {
    mode: 'light',
    primary: {
      main: '#3fb57c',
    },
    secondary: {
      main: '#2754b0',
    },
  }
});


